import { Backpack, TriangleAlert } from "lucide-react";
import React from "react";
import { calculateOneRingJourneyState } from "@shared/rules-engine";

export function OneRingJourneyState({ endurance, load, onLoadChange }: { endurance: number; load: number; onLoadChange: (load: number) => void }) {
  const state = calculateOneRingJourneyState(endurance, load);
  return <article className={`border p-5 ${state.weary ? "border-[#d27648]/80 bg-[#3b1d19]/55" : "border-[#83a89a]/35 bg-[#171a18]"}`}><div className="flex items-start justify-between gap-4"><div><p className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#a9c7bb]"><Backpack className="h-4 w-4 text-[#d27648]" /> Estado da jornada</p><h2 className="mt-2 font-serif text-3xl leading-none text-[#f4eee4]">{state.weary ? "Cansado" : "Pronto"}</h2><p className="mt-2 text-xs leading-5 text-[#b8b3a8]">Resistência {state.endurance} · Carga {state.load} · margem {state.margin >= 0 ? `+${state.margin}` : state.margin}</p></div>{state.weary ? <TriangleAlert className="h-6 w-6 shrink-0 text-[#ffb08e]" aria-label="Companheiro cansado" /> : null}</div><label className="mt-4 flex items-center justify-between gap-4 border-t border-white/10 pt-4 text-xs text-[#d8d1c5]"><span>Carga transportada</span><input aria-label="Carga transportada" type="number" min="0" max="99" value={load} onChange={(event) => onLoadChange(Math.max(0, Math.min(99, Number(event.target.value) || 0)))} className="h-9 w-20 border border-white/15 bg-[#101211] px-2 text-right text-[#f4eee4]" /></label>{state.weary ? <p className="mt-4 border-t border-[#ffb08e]/25 pt-3 text-xs leading-5 text-[#ffded1]">A Resistência alcançou a Carga: os resultados <strong>1–3</strong> dos dados de perícia passam a valer zero até que a carga diminua ou a Resistência seja restaurada.</p> : <p className="mt-4 border-t border-white/10 pt-3 text-[10px] text-[#a9c7bb]">O motor marca Cansaço automaticamente se a Resistência for igual ou menor que a Carga.</p>}</article>;
}
